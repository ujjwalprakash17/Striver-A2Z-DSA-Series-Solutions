#include <bits/stdc++.h>
using namespace std;

// Approach :
//  take a set data structure to optimize the finding of element in wordList
//  1. traverse using bfs
//  2. iterate on each word and try to find word which does exists in wordList
//  3. if found push it into queue

int ladderLength(string beginWord, string endWord, vector<string> &wordList)
{
    // take a set data strcuture
    
}

int main()
{

    return 0;
}